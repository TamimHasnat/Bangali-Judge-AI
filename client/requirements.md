## Packages
framer-motion | Page transitions and highly playful bounce animations
html2canvas | Used for generating shareable meme cards from DOM elements

## Notes
- Using neo-brutalism aesthetic with bright colors and stark black borders for a "meme/playful" vibe.
- `POST /api/confessions` is expected to internally generate the AI judgment and save it to the database, returning the full record.
- `html2canvas` will be used to capture the JudgmentCard element.
